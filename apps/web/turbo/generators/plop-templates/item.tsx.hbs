import React from 'react';
import { use{{pascalCase name}}s } from '../context/{{kebabCase name}}-context';

export const {{pascalCase name}}Item: React.FC<{ {{camelCase name}}Id: string }> = ({ {{camelCase name}}Id }) => {
  const { use{{pascalCase name}}Query, update{{pascalCase name}}, delete{{pascalCase name}} } = use{{pascalCase name}}s();

  {{{{raw}}}}
  const { data: {{camelCase name}}, isLoading, error } = use{{pascalCase name}}Query({{camelCase name}}Id);

  if (isLoading) return <div>Loading {{camelCase name}}...</div>;
  if (error) return <div>Error: {error.message}</div>;
  if (!{{camelCase name}}) return null;
  {{{{/raw}}}}

  return (
    <div>
      {/* Display {{camelCase name}} properties here */}
      {{{{raw}}}}
      <button onClick={() => update{{pascalCase name}}({ id: {{camelCase name}}.id, /* Add properties to update */ })}>
        Update {{pascalCase name}}
      </button>
      <button onClick={() => delete{{pascalCase name}}({{camelCase name}}.id)}>Delete {{pascalCase name}}</button>
      {{{{/raw}}}}
    </div>
  );
};